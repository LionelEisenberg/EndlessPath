class_name InventoryList
extends InventoryScrollHost

## InventoryList
## A scrollable single-column list host. All behavior (add_slot/clear_slots/
## get_slots + ScrollRail binding) comes from InventoryScrollHost; the only
## difference from InventoryGrid is that the content container is a
## VBoxContainer, so rows stack full-width in one column.
